services:
  app:
    build: .
    image: property-manager
    container_name: property-manager
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:${SPRING_LOCAL_PORT}:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker,prod
    networks:
      - app-network
networks:
  app-network:
    external: true
